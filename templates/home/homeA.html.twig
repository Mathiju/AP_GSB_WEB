<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Accueil</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        /* Styles généraux */
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background-image: url('https://next-images.123rf.com/index/_next/image/?url=https://assets-cdn.123rf.com/index/static/assets/top-section-bg.jpeg&w=3840&q=75');
            background-size: cover;
            color: #fff;
        }

        /* En-tête */
        header {
            display: flex;
            justify-content: space-between; /* Aligner l'image à gauche et le bouton à droite */
            align-items: center;
            background: linear-gradient(to right, rgba(18, 120, 236, 0.31), rgba(182, 39, 99, 0.51));
            padding: 20px; /* Augmente l'espace intérieur */
        }

        header .logo {
            width: 50px; /* Ajuste la taille de l'image */
            height: auto;
            margin-right: 20px; /* Espace entre l'image et les autres éléments */
        }

        .logout:hover {
            background-color:rgba(177, 178, 233, 0.48); /* Rouge */
        }

        header .logout {
            background-color: rgba(111, 112, 177, 0.48); /* Gris foncé */
            border-color: #6c757d; /* Bordure gris foncé */
            border: none;
            padding: 10px 15px;
            color:rgb(255, 255, 255);
            cursor: pointer;
            font-weight: bold;
            font-size: 1.2rem; /* Agrandit le texte */
            text-decoration: none;
            border-radius: 20px;
        }

        .container {
            justify-content: center;
            align-items: center;
            padding: 20px;
            border-radius: 10px;
            text-align: center;
            background: rgba(50, 50, 50, 0.2);
            margin: 20px auto; /* Centrer horizontalement */
            width: 1000px; /* Ajuste la largeur au contenu */
            max-width: 90%; /* Limite la largeur maximale */
            backdrop-filter: blur(10px); /* Ajoute un effet de flou */
        }

        .box {
            width: 1000px;
            height: 250px;
            background-color:rgba(255, 255, 255, 0.42);
            color: #000;
            display: flex;
            justify-content: center;
            align-items: center;
            font-size: 1.2em;
            font-weight: bold;
            padding: 20px; /* Ajoute de l'espace à l'intérieur de la boîte */
            box-sizing: border-box;
            border-radius: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
        }

        th, td {
            border: 1px solid #000;
            padding: 8px;
            text-align: center;
            background-color: #f2f2f2;
        }

        th {
            background-color:rgb(192, 192, 192);
        }

        /* Pied de page */
        footer {
            position: fixed;
            bottom: 0;
            left: 0;
            padding: 10px;
            color: #fff;
            font-size: 0.9em;
        }

        /* Style des boutons */
        .btn {
            padding: 5px 10px;
            margin: 5px;
            cursor: pointer;
            border-radius: 5px;
            border: none;
        }

        .btn-add {
            background-color: rgb(93, 161, 101);
            color: white;
            border: none;
            padding: 5px 10px;
            cursor: pointer;
            border-radius: 10px;
        }
        .btn-add:hover {
            background-color: rgb(134, 209, 143);
        }
        .btn-approve {
            background-color: rgb(93, 161, 101);
            color: white;
            border: none;
            padding: 5px 10px;
            cursor: pointer;
            border-radius: 10px;
        }
        .btn-approve:hover {
            background-color: rgb(134, 209, 143);
        }
        .btn-reject:hover {
            background-color: rgb(196, 122, 122);
        }
        .btn-reject {
            background-color: rgb(161, 93, 93);
            color: white;
            border: none;
            padding: 5px 10px;
            cursor: pointer;
            border-radius: 10px;
        }
        /* Appliquer un texte noir dans les modales */
        .modal-content {
        color: black; /* Définit la couleur du texte à noir */
        }

        .modal-body {
        color: black; /* Définit la couleur du texte à noir pour le contenu de la modale */
        }

        .modal-header .modal-title {
        color: black; /* Définit la couleur du titre de la modale à noir */
        }

        .modal-footer button {
        color: black; /* Définit la couleur du texte des boutons de la modale */
        }
    </style>
</head>
<body>
    <header>
        <img src="https://img.freepik.com/vecteurs-libre/ordinateur-bureau-vconcept-illustration_114360-12153.jpg?ga=GA1.1.840802286.1729083279&semt=ais_hybrid" alt="Logo" class="logo" style="width: 80px; height: auto; border-radius: 50%;"> <!-- Ajoutez votre image ici -->
        <a href="#" id="logout" class="logout">Se déconnecter</a>
        <script>
            document.getElementById('logout').addEventListener('click', function(event) {
                event.preventDefault(); // Empêche le comportement par défaut du lien
                window.location.href = 'login'; // Redirige vers la route de login
            });
        </script>
    </header>

    <div class="container">
        <h1>Appuyez sur le bouton pour ajouter un utilisateur.</h1>
        <button class="btn btn-add" onclick="window.location.href='add'" id="signin">Ajouter</button>
        <script>
            document.getElementById('signin').addEventListener('click', function(event) {
                event.preventDefault(); // Empêche le comportement par défaut du lien
                window.location.href = 'signin'; // Redirige vers la route de login
            });
        </script>
    </div>

    <p style="text-align: center; font-size: 1.5em;">Liste des utilisateurs :</p>
    <div class="container" id="user-list" style="display: flex; gap: 20px;"></div>

    <!-- Modal Modification -->
    <div class="modal fade" id="modificationModal" tabindex="-1" aria-labelledby="modificationModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="modificationModalLabel">Modifier un utilisateur</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Fermer"></button>
                </div>
                <div class="modal-body">
                    <form id="modificationForm">
                        <div class="mb-3">
                            <label for="nomUserModif" class="form-label">Nom</label>
                            <input type="text" class="form-control" id="nomUserModif" required>
                        </div>
                        <div class="mb-3">
                            <label for="prenomUserModif" class="form-label">Prénom</label>
                            <input type="text" class="form-control" id="prenomUserModif" required>
                        </div>
                        <div class="mb-3">
                            <label for="emailUserModif" class="form-label">Identifiant</label>
                            <input type="email" class="form-control" id="emailUserModif" required>
                        </div>
                        <button type="submit" class="btn btn-primary">Modifier</button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- Importation du fichier Firebase -->
    <script type="module">
        import { initializeApp } from "https://www.gstatic.com/firebasejs/9.6.1/firebase-app.js";
        import { getFirestore, collection, getDocs, updateDoc, doc, deleteDoc } from "https://www.gstatic.com/firebasejs/9.6.1/firebase-firestore.js";

        const firebaseConfig = {
            apiKey: "AIzaSyB24J4yHKRpk59CG-mnLUzJzizIuSo64-Q",
            authDomain: "ap-gsp.firebaseapp.com",
            projectId: "ap-gsp",
            storageBucket: "ap-gsp.firebasestorage.app",
            messagingSenderId: "817436331010",
            appId: "1:817436331010:web:b4f49704fd32d367bf6b1b"
        };

        const app = initializeApp(firebaseConfig);
        const db = getFirestore(app);
        let currentUserId = null;

        async function fetchUsers() {
            try {
                const querySnapshot = await getDocs(collection(db, "user"));
                const userListContainer = document.getElementById('user-list');
                
                querySnapshot.forEach((docSnapshot) => {
                    const user = docSnapshot.data();
                    const userId = docSnapshot.id;
                    const userDiv = document.createElement('div');
                    userDiv.style.textAlign = 'center';
                    userDiv.style.margin = '10px';
                    userDiv.style.border = '1px solid #ccc';
                    userDiv.style.padding = '10px';
                    userDiv.style.borderRadius = '10px';
                    
                    const img = document.createElement('img');
                    img.src = user.profilePicture || 'https://us.123rf.com/450wm/flatdesign/flatdesign2009/flatdesign200900117/154641062-ic%C3%B4ne-de-groupe-de-personnes-conception-de-sites-web-vectoriels.jpg?ver=6';
                    img.style.width = '100px';
                    img.style.borderRadius = '50%';
                    
                    const userInfo = document.createElement('div');
                    userInfo.innerHTML = `
                        <strong>Nom :</strong> ${user.nom} <br>
                        <strong>Prénom :</strong> ${user.prenom} <br>
                        <strong>Identifiant :</strong> ${user.identifiant} <br>
                    `;

                    const editButton = document.createElement('button');
                    editButton.innerText = 'Modifier';
                    editButton.classList.add('btn', 'btn-custom-modifier');
                    editButton.onclick = function() {
                        currentUserId = userId;
                        document.getElementById('nomUserModif').value = user.nom;
                        document.getElementById('prenomUserModif').value = user.prenom;
                        document.getElementById('emailUserModif').value = user.identifiant;
                        new bootstrap.Modal(document.getElementById('modificationModal')).show();
                    };

                    const deleteButton = document.createElement('button');
                    deleteButton.innerText = 'Supprimer';
                    deleteButton.classList.add('btn', 'btn-custom-supprimer');
                    deleteButton.onclick = async function() {
                        if (confirm(`Voulez-vous vraiment supprimer ${user.nom} ?`)) {
                            await deleteDoc(doc(db, "user", userId));
                            userListContainer.removeChild(userDiv);
                        }
                    };

                    userDiv.appendChild(img);
                    userDiv.appendChild(userInfo);
                    userDiv.appendChild(editButton);
                    userDiv.appendChild(deleteButton);
                    userListContainer.appendChild(userDiv);
                });
            } catch (error) {
                console.error('Erreur lors de la récupération des utilisateurs:', error);
            }
        }

        document.getElementById('modificationForm').addEventListener('submit', async function(event) {
            event.preventDefault();
            if (currentUserId) {
                await updateDoc(doc(db, "user", currentUserId), {
                    nom: document.getElementById('nomUserModif').value,
                    prenom: document.getElementById('prenomUserModif').value,
                    email: document.getElementById('emailUserModif').value
                });
                location.reload();
            }
        });

        window.onload = fetchUsers;
    </script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>